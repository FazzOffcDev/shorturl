<!DOCTYPE html>
<html lang="en" data-bs-theme="dark"> 
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 | Link Tidak Ditemukan</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css"> 
</head>
<body class="bg-body-tertiary d-flex align-items-center justify-content-center" style="height: 100vh;">
    
    <div class="text-center transparent-card p-5 mx-auto" style="max-width: 600px;">
        <h1 class="display-1 text-danger">404</h1>
        <h2 class="text-light mb-4">Link Tidak Ditemukan</h2>
        
        <% if (typeof shortId !== 'undefined') { %>
            <p class="lead text-secondary">
                Link pendek <code>/<%= shortId %></code> tidak ada di database kami atau telah kedaluwarsa.
            </p>
        <% } else { %>
            <p class="lead text-secondary">
                Halaman yang Anda cari tidak ada.
            </p>
        <% } %>

        <a href="/" class="btn btn-warning btn-lg mt-3">Kembali ke Beranda</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>