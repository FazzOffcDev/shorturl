<!DOCTYPE html>
<html lang="en" data-bs-theme="dark"> 
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> ShortURL | FazzCodex</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css"> 
</head>
<body class="bg-body-tertiary">
<body class="bg-body-tertiary">
    
    <% if (notification) { %>
        <div class="toast-container position-fixed bottom-0 end-0 p-3">
            <div id="liveToast" class="toast align-items-center text-white bg-<%= notification.type %> border-0" role="alert" aria-live="assertive" aria-atomic="true">
                <div class="d-flex">
                    <div class="toast-body">
                        <%= notification.message %>
                    </div>
                    <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
                </div>
            </div>
        </div>
        <script>
            // Inisialisasi dan tampilkan Toast
            document.addEventListener('DOMContentLoaded', function() {
                var toastEl = document.getElementById('liveToast');
                var toast = new bootstrap.Toast(toastEl);
                toast.show();
            });
        </script>
    <% } %>

    <div class="container my-5">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
    <div class="container my-5">
        <h1 class="text-center text-light mb-2">🚀 Url Short</h1>
        <p class="text-center text-secondary mb-5">Selamat datang, **<%= currentUser %>**. Semua link Anda terdaftar di bawah.</p>

        <div class="card transparent-card mx-auto mb-5" style="max-width: 700px;">
            <div class="card-header border-0 pb-0">
                <h4 class="card-title text-light">Pendekkan URL Baru</h4>
            </div>
            <div class="card-body">
                <form action="/shortUrls" method="POST" class="input-group">
                    <input required 
                        placeholder="Masukkan URL yang panjang di sini (cth: https://contoh.com)" 
                        type="url" 
                        name="fullUrl" 
                        id="fullUrl" 
                        class="form-control form-control-lg"
                        style="background-color: rgba(52, 58, 64, 0.6); border-color: rgba(255, 255, 255, 0.2); color: white;"
                    >
                    <button type="submit" class="btn btn-primary btn-lg">Pendekkan</button>
                </form>
            </div>
        </div>

        <h2 class="text-light mt-5 mb-3">Daftar Link Saya</h2>
        <div class="table-responsive">
            <table class="table table-dark table-striped table-hover transparent-card">
                <thead>
                    <tr class="text-info">
                        <th>URL Penuh</th>
                        <th>URL Pendek</th>
                        <th class="text-center">Klik</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (urlDatabase.length === 0) { %>
                        <tr>
                            <td colspan="3" class="text-center text-secondary">Belum ada URL yang dipendekkan.</td>
                        </tr>
                    <% } %>
                    <% urlDatabase.forEach(url => { %>
                        <tr>
                            <td style="max-width: 400px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;">
                                <a href="<%= url.fullUrl %>" target="_blank" class="text-info small" title="<%= url.fullUrl %>"><%= url.fullUrl %></a>
                            </td>
                            <td>
                                <a href="/<%= url.shortId %>" target="_blank" class="text-warning fw-bold">
                                    / <%= url.shortId %>
                                </a>
                            </td>
                            <td class="text-center text-light"><%= url.clicks %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>